{
  "version": 0,
  "installCommand": "npm install",
  "buildCommand": "ng build",
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/ng-blog"
      }
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
